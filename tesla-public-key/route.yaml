apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tesla-public-key
spec:
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  to:
    kind: Service
    name: tesla-public-key
    weight: 100
  port:
    targetPort: http
